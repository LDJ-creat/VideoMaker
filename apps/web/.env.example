# FastAPI backend (server-only — used by Next.js BFF proxy, not exposed to browser)
VIDEOMAKER_API_URL=http://127.0.0.1:8000

# When true, BFF returns fixture JSON if upstream is unreachable (development demos only)
VIDEOMAKER_USE_FIXTURE_FALLBACK=false
