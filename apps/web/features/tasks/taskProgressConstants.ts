export const SSE_FAILURE_THRESHOLD = 3;
export const POLL_INTERVAL_MS = 3000;
/** Poll alongside SSE so terminal status appears without waiting for stream events. */
export const SSE_ACTIVE_POLL_INTERVAL_MS = 3000;
