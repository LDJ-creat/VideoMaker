[project]
name = "videomaker-composition"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.23",
  "referencing>=0.35",
  "pyyaml>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "../shared"]
