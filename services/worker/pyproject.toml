[project]
name = "videomaker-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.23",
  "referencing>=0.35",
  "faster-whisper>=1.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
